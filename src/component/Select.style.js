import styled from "styled-components";

export const StorySelectContainer = styled.div`
    display: flex;
    align-items: center;
    background-color: ${(props) => props.theme.menuColor};
    justify-content: space-around;
    border-radius: 5px;
    width: 220px;
    height: 70px;
`